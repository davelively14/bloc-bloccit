<h1><%= @topic.name %></h1>

<%= link_to "Edit Topic", edit_topic_path, class: 'btn btn-success' %>
<%= link_to "Delete Topic", @topic, method: :delete, class: 'btn btn-danger', data: {confirm: "Are you sure you want to delete this topic?"} %>

<div class="row">
  <div class="col-md-8">
    <p class="lead"><%= @topic.description %></p>
    <% @topic.posts.each do |post| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-header">
            <%= link_to post.title, topic_post_path(@topic, post) %>
          </h4>
          <small>
            submitted <%= time_ago_in_words(post.created_at) %> ago<br>
            <%= post.comments.count %> comments
          </small>
        </div>
      </div>
    <% end %>
    <% if @topic.sponsored_posts.length > 0 %>
      <div style="background-color: rgb(161, 255, 249); padding: 5px; margin: 20px 0; border-radius: 5px">
        <h4><small>Sponsored content</small></h6>
        <% @topic.sponsored_posts.each do |post| %>
        <div class="media">
          <div class="media-body">
            <h4 class="media-header">
              <%= link_to post.title, topic_sponsored_post_path(@topic, post) %>
            </h4>
            <small>
              submitted <%= time_ago_in_words(post.created_at) %> ago<br>
            </small>
          </div>
        </div>
        <% end %>
      </div>
    <% end %>
  </div>
  <div class="col-md-4">
    <%= link_to "New Post", new_topic_post_path(@topic), class: 'btn btn-success' %>
    <%= link_to "New Sponsored Post", new_topic_sponsored_post_path(@topic), class: 'btn btn-success' %>
  </div>
</div>
