<h1>All Advertisements <%= link_to "New Ad", new_advertisement_path, class: 'btn btn-success' %></h1>
<% @advertisements.each do |ad| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to ad.title, ad %>
      </h4>
    </div>
  </div>
<% end %>
